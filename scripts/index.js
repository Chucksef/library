let myLibrary = [];

class Book {
    constructor(title, author, pubDate, owned="true", read="false") {
        self.title = title;
        self.author = author;
        self.pubDate = pubDate;
        self.owned = owned;
        self.read = read;
    }
}

function addBookToLibrary() {
  // do stuff here
}