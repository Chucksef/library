let myLibrary = [];

class Book {
    constructor(title, author, pubDate, status="owned") {
        self.title = title;
        self.author = author;
        self.pubDate = pubDate;
        self.status = status;
    }
}

function addBookToLibrary() {
  // do stuff here
}